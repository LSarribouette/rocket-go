{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(profileForm) }}
    {{ form_widget(profileForm) }}
    {{ form_end(profileForm) }}
    <a href="{{ path('home_profile') }}">< Retour</a>
{% endblock %}

{% block title %}{{ parent() }} - Modifier mon profil{% endblock %}

